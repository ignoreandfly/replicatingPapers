# Everything in this package is hand-written, by me, first.
#
# Claude Code may add scaffolding here — signatures, docstrings, shape
# annotations, `raise NotImplementedError` bodies — and nothing else. No
# working forward pass, no "fix", no refactor, unless I say `take the wheel`.
#
# See CLAUDE.md, "The contract".
